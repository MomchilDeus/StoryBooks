<nav class="blue-grey darken-4">
    <div class="container">
        <div class="nav-wrapper">
            <a href="/stories" class="brand-logo center">Storybooks</a>
            {{!-- Show on large so that it's part of the UI regardless of the screen size --}}
            <a href="#" class="button-collapse show-on-large" data-activates="main-menu"><i class="fa fa-bars"></i></a>
            {{!-- Right Nav --}}
            <ul class="right hide-on-small-only">
                {{!-- if user means if we're logged in --}}
                {{#if user}}
                    <li>
                        <a href="/dashboard">Welcome {{user.firstName}}</a>
                    </li>
                    <li>
                        <a href="/auth/logout"><i class="fa fa-sign-out"></i> Logout</a>
                    </li>
                {{else}}
                    <li>
                        <a href="/stories"><i class="fa fa-book"></i> Public stories</a>
                    </li>
                {{/if}}
            </ul>
            {{!-- Side menu --}}
            <ul class="side-nav" id="main-menu">
                {{#if user}}

                {{else}}
                <li>
                    <a href="/auth/google" class="btn red darken-1"><i class="fa fa-google left"></i> Login with Google</a>
                </li>
                {{/if}}
                <li>
                    <a href="/stories"><i class="fa fa-book"></i> Public Stories</a>
                </li>
                {{!-- Only show if logged in --}}
                {{#if user}}
                <li class="divider"></li>
                <li>
                    <a href="/dashboard"><i class="fa fa-cog"></i> Dashboard</a>
                </li>
                <li>
                    <a href="/my"><i class="fa fa-user"></i> My Stories</a>
                </li>
                <li>
                    <a href="/auth/logout"><i class="fa fa-sign-out"></i> Log out</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>